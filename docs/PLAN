下面是一份可直接放进 **opencode-pilotdeck** 仓库的《工程计划文档（Agent 执行版）》。内容聚焦 **插件本体**：事件采集、token usage 归因与上报、`pilotdeck.*` tools、Rules/Protocol 注入（可配置、可节流），不涉及 PilotDeck Server/桌面端实现。

建议落盘为：

* `docs/PLAN.md`
* `docs/TICKETS.md`
* `docs/DEFINITION_OF_DONE.md`

我按这三个文件的结构给你（你也可以先只建 `docs/PLAN.md`）。

---

# docs/PLAN.md — opencode-pilotdeck 工程计划文档（Agent 执行版）

## 0. 一句话目标

`opencode-pilotdeck` 是 OpenCode 的插件：在 **不改变用户使用 OpenCode 习惯** 的前提下，做到：

1. **全覆盖采集**：捕获会话生命周期、workspace/project、模型与 token usage（或等价 usage）
2. **可审计上报**：把 runs/events/actions 以幂等方式写入 PilotDeck Server
3. **可控写回**：通过 `pilotdeck.*` tools + Rules/Protocol，让 Agent 在关键节点更新进度（不刷屏）

> 关键边界：插件不负责 PilotDeck Server 的任何业务逻辑；只负责 **采集、协议注入、上报与工具桥接**。

---

## 1. MVP 范围（必须有）

### 1.1 配置与连接

* 读取配置（环境变量 / opencode 配置 / 插件配置文件，择一实现）
* PilotDeck Server URL
* Token（Admin 或 Agent token）
* 默认 project 归因策略（至少支持 worktree path -> projectId 映射）

### 1.2 事件采集与 usage 上报（核心）

* 订阅 OpenCode 的会话生命周期事件（至少捕获“会话结束/idle”）
* 在 idle 时结算并上报 usage（如果无法直接拿 usage，先上报 session 元信息，usage 后置也行，但必须留接口）
* 创建或更新 PilotDeck 的 run（幂等）

### 1.3 tool：让 Agent “自然写回”

最小集合（MVP）：

* `pilotdeck.event`：append-only 写事件（用于 timeline 审计）
* `pilotdeck.action`：提出语义化 action（可由 Web 审查确认；或直接调用 PilotDeck 的 agent/actions endpoint）

可选（V1）：

* `pilotdeck.run.start` / `pilotdeck.run.finish`（更明确的 run 生命周期）

### 1.4 Rules/Protocol 注入（节流核心）

* 提供一段 **PilotDeck Protocol**（短规则）告诉 Agent：

  * 何时写回（开始、状态变化、结束）
  * 禁止逐步碎碎念
* MVP 可以仅提供模板文件 + 在日志里提示用户如何启用
* V1 再做“自动注入/维护”（可配置：全局/项目级）

---

## 2. 非目标（明确不做，防膨胀）

* 不做 PilotDeck Server 的 schema/接口演进
* 不做 Desktop 的安装器（可以提供文档让 Desktop 去安装）
* 不做完整对话 UI
* 不做复杂的多用户权限体系（只处理 token 头）

---

## 3. 交付里程碑

### M0：工程骨架与可运行插件（Day 1）

交付：

* 插件项目结构、构建、发布（npm/pnpm/bun 其一）
* 最小加载：插件能被 OpenCode 识别并输出日志
* 配置读取与校验（server url、token）

验收：

* OpenCode 启动时插件加载成功、日志可见
* `PILOTDECK_SERVER_URL` 缺失时给出明确错误提示

---

### M1：采集会话生命周期 + 上报 run（Day 2-4）

交付：

* 订阅 session 事件（至少 idle）
* 采集：sessionId、project/worktree、model、开始/结束时间
* PilotDeck 上报：创建 run（幂等键基于 sessionId）

验收：

* 触发一次 OpenCode 会话后，PilotDeck 中出现对应 run（不重复创建）

---

### M2：usage/token 结算与 cost 归因（Day 5-7）

交付：

* 在 session idle 时获取 usage（输入/输出 tokens、模型、费用如可得）
* 将 usage patch 到 run（或写入 run.payload_json）
* 降级策略：usage 缺失时也能上报“unknown”，并记录原因

验收：

* 至少能在 PilotDeck run 里看到 usage 字段（或明确缺失原因）

---

### M3：pilotdeck.* tools（Day 8-10）

交付：

* 注册 `pilotdeck.event` tool：写 timeline event（append-only + 幂等）
* 注册 `pilotdeck.action` tool：写语义 action（幂等）
* 工具权限与安全：默认需要 token；失败返回可读错误

验收：

* 在 OpenCode 对话里调用 tool，PilotDeck timeline 立刻出现 event/action
* 幂等：相同 idempotencyKey 重复调用不产生重复记录

---

### M4：Rules/Protocol 注入与节流（Day 11-14）

交付：

* Protocol 模板 + “自动注入”选项（可配置启用/禁用）
* 节流策略：

  * 仅关键节点写回（start/finish/status change）
  * 对频繁事件做 debounce（例如 30s 只写一次摘要）
* 可观测性：本地日志/调试开关

验收：

* 开启 protocol 后，Agent 不会刷屏写 event；只在关键节点写
* 关闭 protocol 后，插件不影响用户现有行为

---

## 4. 对接 PilotDeck Server 的契约（插件侧只消费）

最少需要明确这些接口（以你当前 PilotDeck 现状为准）：

* `POST /api/agent/runs`（幂等 create）
* `PATCH /api/agent/runs/:id`（写入 status/usage）
* `POST /api/agent/events`（append-only）
* `POST /api/agent/actions`（语义化 action）
* header：

  * `X-PM-Agent-Token`（优先）
  * 或 `X-PM-Token`（admin）

> 建议插件内做一个 `PilotDeckClient`：统一封装鉴权、重试、超时、错误映射。

---

## 5. 关键设计决策（建议默认值）

### 5.1 runId/幂等键策略

* run 的幂等主键：`source=opencode` + `sessionId`
* event/action 幂等键：`sessionId + type + hash(payload)` 或显式传入 `idempotencyKey`

### 5.2 project 归因策略（MVP）

优先顺序：

1. 插件 ctx 提供的 worktree path（或 project）
2. 用户配置映射：`WORKTREE -> PILOTDECK_PROJECT_ID`
3. fallback：写入 Inbox 项目（或创建“Unassigned”）

### 5.3 上报时机

* run：session start（可选）+ session idle（必须）
* events/actions：只有在 tool 被调用时写（MVP），或协议触发时写（V1）

---

## 6. 质量与可观测性

* 日志级别：info/debug
* 失败处理：

  * PilotDeck 不可达：缓存到本地队列文件（V1）或仅打日志（MVP）
  * 上报重试：指数退避 + 最大次数
* 测试：

  * 单元测试：client、幂等、payload 规范化
  * 集成测试（可选）：对一个 mock server 发请求验证

---

# docs/TICKETS.md — 任务拆分清单（可直接分配给 Agent）

## T0 工程骨架

* T0.1 初始化插件项目（TS/ESM、build、lint、test）
* T0.2 插件入口：加载成功日志 + 版本打印
* T0.3 配置系统：读取 env + 配置校验 + 默认值

## T1 事件采集与 run 上报

* T1.1 订阅 session lifecycle（至少 idle）
* T1.2 从 ctx 提取：sessionId/worktree/project/model/timestamps
* T1.3 PilotDeckClient：`createRunIdempotent(sessionId, payload)`
* T1.4 错误映射与重试

## T2 usage/token 结算

* T2.1 idle 时拉取 usage（从 session/detail 或 stats）
* T2.2 patch run 写入 usage/cost
* T2.3 usage 缺失降级：写 event 记录原因（可选）

## T3 tools

* T3.1 注册 tool：`pilotdeck.event`
* T3.2 注册 tool：`pilotdeck.action`
* T3.3 幂等实现：idempotencyKey/exists 检查
* T3.4 文档：tool schema、示例调用

## T4 protocol

* T4.1 `docs/protocol-pilotdeck.md`（短规则）
* T4.2 自动注入开关（全局/项目级/禁用）
* T4.3 节流：debounce/聚合策略
* T4.4 兼容性：不破坏用户已有 rules

## T5 发布与文档

* T5.1 README：安装、配置、常见问题
* T5.2 changelog/versioning
* T5.3 示例：如何在 OpenCode 中启用 + 如何验证 PilotDeck 收到数据

---

# docs/DEFINITION_OF_DONE.md — 验收标准

## MVP Done

* 插件可加载、可配置
* session idle 后必定产生 run 上报（至少一次）
* usage 若可得则写入；不可得则清晰记录原因
* `pilotdeck.event` tool 可用（写入 event）
* `pilotdeck.action` tool 可用（写入 action）
* 幂等：重复上报不会产生重复 run/event/action
* 错误可读：token 错、server 不可达、接口 4xx/5xx 有明确提示

## V1 Done（可后置）

* 自动 protocol 注入（可配置）
* 本地离线队列与补偿上报
* 基本集成测试（mock server）

---

## 你现在可以怎么用它（不需要我再问你问题）

1. 直接把三段内容分别保存到 `docs/PLAN.md`、`docs/TICKETS.md`、`docs/DEFINITION_OF_DONE.md`
2. 让 Agent 从 **T0.1** 开始做骨架，然后并行分配：

   * Agent A：T1/T2（采集+usage）
   * Agent B：T3（tools）
   * Agent C：T4（protocol）

如果你把 **opencode-pilotdeck** 当前已有的目录结构/语言栈（TS or JS、构建方式）贴一下，我也可以把这些 tickets 进一步落到“具体文件名/模块名/代码骨架”，让 Agent 能按仓库现状直接开干。
